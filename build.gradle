apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'jetty'

group = 'io.qdb.buffer'
version = '0.9.0'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.10'
}

task sourceJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

artifacts {
    archives sourceJar
}

